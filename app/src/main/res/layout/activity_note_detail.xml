<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NoteDetailActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/activity_note_details_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="?attr/activityNoteDetailsToolbarColor">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/activity_note_details_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:titleTextColor="?attr/primaryTextColor"
            app:title="@string/activity_note_details_label"
            app:popupTheme="?attr/toolbarPopupTheme" />

    </com.google.android.material.appbar.AppBarLayout>

    <include layout="@layout/scroll_deneme" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/activity_note_details_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:backgroundTint="?attr/primaryButtonColor"
        app:srcCompat="@drawable/ic_add_icon_24dp"
        app:tint="@android:color/white"
        app:borderWidth="0dp"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>